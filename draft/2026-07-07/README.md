# 2026-07-07 Draft Snapshots

- `original_monolithic_main.tex`: the pre-cleanup manuscript, preserved exactly as the original single-file LaTeX draft from `paper/main.tex`.
- `current_split_snapshot/`: the organized split manuscript snapshot created before substantive rewriting. It includes `main.tex`, `refs.bib`, `AGENTS.md`, and numbered `sections/`.
- `sec*.md`: earlier Markdown draft material from before the LaTeX workspace cleanup.

Use the root `main.tex` and `sections/` for current work. Use this folder only for rollback or comparison.
